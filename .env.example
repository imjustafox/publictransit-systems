# URL for the transit incidents Cloudflare Worker
INCIDENTS_WORKER_URL=https://transit-incidents.your-subdomain.workers.dev

# CARTO basemap API key (client-side, inlined at build). Without it the map
# tiles render with an "API key required" watermark.
NEXT_PUBLIC_CARTO_API_KEY=your-carto-key
