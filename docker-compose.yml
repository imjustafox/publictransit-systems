services:
  app:
    networks:
      - webserver_proxynet
    image: ghcr.io/imjustafox/publictransit-systems:main
    container_name: publictransit-app
    environment:
      - INCIDENTS_WORKER_URL=https://transit-incidents.capitolhostingsolutions.workers.dev
    restart: unless-stopped

networks:
  webserver_proxynet:
    external: true
